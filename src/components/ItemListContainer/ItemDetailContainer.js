import ItemDetail from "./ItemDetailContainer/ItemDetail";

const ItemDetailContainer = ({ Preview, Open, Details }) => {
    return <ItemDetail Preview={Preview} Open={Open} Details={Details} />;
};

export default ItemDetailContainer;
